# =============================================================================
# SVC Fall '25 Testing Challenge - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Tests work with mocks - these are only needed for manual testing/production
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Production database (Neon Postgres or any PostgreSQL)
DATABASE_URL=postgresql://user:password@your-host.neon.tech/your-database?sslmode=require

# Test database (auto-configured by Testcontainers, override if needed)
# Leave empty to use Testcontainers or pg-mem fallback
TEST_DATABASE_URL=

# MongoDB (if using MongoDB features)
MONGODB_URI=

# -----------------------------------------------------------------------------
# REDDIT API (for social qualification verification)
# -----------------------------------------------------------------------------
# Get credentials at: https://www.reddit.com/prefs/apps
# Create a "script" type app for testing
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here

# -----------------------------------------------------------------------------
# SUPABASE (for authentication)
# -----------------------------------------------------------------------------
# Get from: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Site URL for redirects and callbacks
VITE_SITE_URL=http://localhost:8080

# Custom ping message (optional)
PING_MESSAGE=pong

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - All VITE_ prefixed variables are exposed to the frontend
# - Never commit your actual .env file with real credentials
# - Tests mock all external APIs, so these aren't required for testing
# - For CI/CD, set these as GitHub Secrets or environment variables
# =============================================================================
